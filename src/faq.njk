---
layout: default
permalink: /faq/
description: "Частые вопросы Дмитрию Бартошевичу о системном маркетинге, бренде, честности и условиях сотрудничества с агентством BARSAN."
title: "FAQ: Дмитрий Бартошевич — подход, опыт и основание BARSAN"
name: "FAQ: Сотрудничество с Дмитрием Бартошевичем"
date:   2015-07-31
last_modified_at: 2026-03-21
eleventyNavigation:
  key: faq
  title: "FAQ"
  parent: home

---

<header class="max-width-text row-gap--xs">
    <p class="section-kicker">FAQ</p>
    <h1 class="intro">
        <span class="h1-name">Вопросы Дмитрию Бартошевичу</span>
        <span class="h1-role">о&nbsp;подходе в&nbsp;маркетинге, сотрудничестве и&nbsp;агентстве BARSAN.</span>
    </h1>
</header>

<div class="button-container">
    <a href="mailto:dmitry@bartoshevich.by" class="button-primary">
        Задать вопрос
    </a>
</div>


<nav class="toc">
    <h2 class="toc__title">Содержание</h2>
    <ul class="additive-spacing">
        {% for section in faq.sections %}
        <li>
            <span class="tocnumber">{{ loop.index }} </span>
            <a class="link bold" href="#{{ section.id }}">
                {{ section.title }}
            </a>
        </li>
        {% for item in section.items %}
        <li class="level2">
            <span class="tocnumber">{{ loop.parent.loop.index }}.{{ loop.index }} </span>
            <a class="link" href="#{{ item.id }}">
                {{ item.question | nbspPrepositions | safe }}
            </a>
        </li>
        {% endfor %}
        {% endfor %}
    </ul>
</nav>


{% for section in faq.sections %}
<section class="row-gap--m space-section">

    <h2 class="heading--gamma" id="{{ section.id }}">{{ section.title }}</h2>

    {% for item in section.items %}
    <article class="additive-spacing{% if not loop.first %} space-block{% endif %}" id="{{ item.id }}">
        <h3 class="heading--delta">{{ item.question | nbspPrepositions | safe }}</h3>
        {{ item.answer | safe }}
    </article>
    {% endfor %}

</section>
{% endfor %}
