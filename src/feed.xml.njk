---
permalink: "/feed.xml"
layout: null
title: "Блог Дмитрия Бартошевича о маркетинге и стратегии"
description: "Кейсы, анализ данных, тренды. Экспертные советы и инсайты для профессионалов."
language: "ru"
author: "Дмитрий Бартошевич"
---
<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0"
     xmlns:dc="http://purl.org/dc/elements/1.1/"
     xml:base="{{ site.url | addPathPrefixToFullUrl }}"
     xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ title }}</title>
    <link>{{ site.url | addPathPrefixToFullUrl }}</link>
    <atom:link href="{{ permalink | htmlBaseUrl(site.url) }}" rel="self" type="application/rss+xml" />
    <description>{{ description }}</description>
    <language>{{ language }}</language>

    {%- for post in collections.post | reverse %}
      {%- set absolutePostUrl = post.url | htmlBaseUrl(site.url) %}
    <item>
      <title>{{ post.data.title }}</title>
      <link>{{ absolutePostUrl }}</link>
      <description>{{ post.data.description | decodeEntities | safe }}</description>
      <pubDate>{{ post.date | dateToRfc822 }}</pubDate>
      <dc:creator> {{ author }} </dc:creator>
      <guid>{{ absolutePostUrl }}</guid>
    </item>
    {%- endfor %}

  </channel>
</rss>
